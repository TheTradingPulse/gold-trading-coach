# V4 Evidence Integrity & Calibration
Canonical setup_score is 0-100; user-facing score is 0-10. Both are now stored explicitly.
Research tiers preserve the live philosophy: 8.5+ Watch, 9.0+ Elite, plus structural gates.
NOT_TRIGGERED is not a loss. MFE/MAE are stored as points and R. Target R is derived from price geometry, never target ordinal.
Target touches are achieved_r, not assumed realized P&L. Same-bar stop+target ambiguity is explicitly flagged and conservatively stop-first.
Evidence V2 is isolated in research_data/v4/evidence_v2.db; prior V1 evidence is preserved but not used for Professor learning.
No dashboard, TradingView, live Elite, journal, or canonical scoring engine is modified.
